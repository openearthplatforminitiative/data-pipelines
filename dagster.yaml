run_queue:
  # This limits all assets to 1 concurrent run...
  # max_concurrent_runs: 1
  
  # ...but we can also limit by tag
  tag_concurrency_limits:
   - key: "dagster/concurrency_key"
     value: "conc_test"
     limit: 1